/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'avatars.githubusercontent.com',
      'randomuser.me',
      'via.placeholder.com'
    ],
  },
}

module.exports = nextConfig
